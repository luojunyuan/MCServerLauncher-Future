<UserControl
    x:Class="MCServerLauncher.WinUI.View.Features.CreateInstance.Providers.CreateInstanceProviderPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">
    <Grid RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="BackButton" Content="{x:Bind Texts['Back'], Mode=OneWay}" Click="Back_Click" />
            <TextBlock Grid.Column="1" Text="{x:Bind Texts['PreFinishCreateInstanceTip'], Mode=OneWay}" Style="{StaticResource SectionHeaderTextBlockStyle}" VerticalAlignment="Center" Margin="8,0,0,0" />
            <Button x:Name="FinishButton" Grid.Column="2" Content="{x:Bind Texts['Continue'], Mode=OneWay}" Click="Finish_Click" IsEnabled="False" />
        </Grid>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="StepsPanel" Spacing="6" Padding="0,0,8,24" />
        </ScrollViewer>
    </Grid>
</UserControl>
